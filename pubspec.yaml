name: system_date_time_format_hook
description: A hook for system_date_time_format plugin
version: 0.1.0
repository: https://github.com/Nikoro/system_date_time_format_hook
issue_tracker: https://github.com/Nikoro/system_date_time_format_hook/issues

environment:
  sdk: '>=3.2.6 <4.0.0'
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_hooks: ^0.20.5
  system_date_time_format: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  mocktail: ^1.0.3